plugin.tx_cpsitproposal_app {
    view {
        # cat=plugin.tx_tx_cpsitproposal_app/view/10; type=string; label=Path to template root (FE)
        templateRootPath =
        # cat=plugin.tx_tx_cpsitproposal_app/view/20; type=string; label=Path to template layouts (FE)
        layoutRootPath =
        # cat=plugin.tx_tx_cpsitproposal_app/view/30; type=string; label=Path to template partials (FE)
        partialRootPath =
    }
}
